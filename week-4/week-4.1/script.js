const express = require("express");
const app = express();

//dom (document)
//need implemnetd in runtime browser & not in ECMA specification
/* 
document-> Is DOM(it has wierd doc)
but this gives you a bunch of functions-> is effectively what DOM is 
& is what we are going to manipulate the DOM
It is an API provied by browser(V8 engine)-> we use to modify HTMl
In tree -> document is at the root

Window-> document(which consist HTMl representation) -> call DOM
*/
